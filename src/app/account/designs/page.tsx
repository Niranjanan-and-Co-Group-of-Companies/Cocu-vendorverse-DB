
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AccountDesignsPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Saved Designs</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Your saved product customizations will appear here.</p>
            </CardContent>
        </Card>
    );
}
